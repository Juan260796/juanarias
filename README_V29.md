# JUAN CUENTAS v29

Correcciones del panel del cliente:

- Se eliminó el texto visible "El contador conserva los servicios vencidos archivados".
- El cliente ya no ve el proveedor dentro de sus credenciales.
- Las promociones activas y vigentes se cargan mediante una ruta segura del servidor y se muestran a todos los clientes autenticados, evitando bloqueos por políticas RLS antiguas.

No requiere migración SQL nueva.
